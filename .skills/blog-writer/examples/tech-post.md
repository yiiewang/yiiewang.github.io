# 技术原理类范例：PBFT 共识算法

> **原始文件**：`docs/blog/posts/2025/03/12-1.md`
> **类型**：技术原理 + 论文精读
> **字数**：约 8000 字
> **特色**：犀利的个人评论 + 大量 `!!! tip` 提炼 + 论文原文引用

---

```markdown
---
date: 2025-03-12
authors:
  - cloaks
categories:
  - 区块链
tags:
  - 区块链
  - PBFT
  - 共识算法
comments: true
description: 深入解析 PBFT 实用拜占庭容错算法的原理与实现，包括三阶段协议和视图变更机制。
---

# PBFT（Practical Byzantine Fault Tolerance）共识算法

PBFT是 Practical Byzantine Fault Tolerance 的缩写，意为实用拜占庭容错算法。该算法是 Miguel Castro（卡斯特罗）和Barbara Liskov（利斯科夫）在 1999 年提出来的，解决了原始拜占庭容错算法效率不高的问题，将算法复杂度由指数级降低到多项式级，使得拜占庭容错算法在实际系统应用中变得可行。该论文发表在1999年的操作系统设计与实现国际会议上（[OSDI99](https://www.alipan.com/s/smLoCoGMbci)）。没错，这个 Loskov 就是提出著名的里氏替换原则（LSP）的人，2008年图灵奖得主。

<!-- more -->

## 摘要部分

!!! quote inline 

    ![](images/image-1.png)

（[OSDI99](https://www.alipan.com/s/smLoCoGMbci)）这篇论文描述了一种副本复制（replication）算法解决拜占庭容错问题。作者认为拜占庭容错算法将会变得更加重要，因为恶意攻击和软件错误的发生将会越来越多，并且导致失效的节点产生任意行为。而早期的拜占庭容错算法或者基于同步系统的假设，或者由于性能太低而不能在实际系统中运作。这篇论文中描述的算法是实用的，因为该算法可以工作在异步环境中，并且通过优化在早期算法的基础上把响应性能提升了一个数量级以上。作者使用这个算法实现了拜占庭容错的网络文件系统（NFS），性能测试证明了该系统仅比无副本复制的标准 NFS 慢了 $3\%$。

## 1.概要介绍

第一段大片废话就是说明拜占庭算法越来越重要了，然后说这篇论文提出解决拜占庭容错的 **状态机副本复制（state machine replication）算法**。这个算法在保证活性和安全性（liveness & safety）的前提下提供了  $\frac{(n - 1)}{3}$ 的容错性。

!!! tip 

    一句话概括：PBFT 容忍 $f$ 个拜占庭节点，集群规模 $n \geq 3f+1$。

从 Lamport 教授在1982年提出拜占庭问题开始，已经有一大堆算法去解决拜占庭容错了。但是一句话概括：这些算法都是狗屎！PBFT 算法跟这些妖艳贱货完全不同，在只读操作中只使用 1 次消息往返（message round trip），在只写操作中只使用 2 次消息往返，并且在正常操作中使用了消息验证编码（Message Authentication Code,简称 MAC），而造成妖艳贱货性能低下的公钥加密（public-key cryptography）只在发生失效的情况下使用。

## 2.系统模型

!!! tip 

    这部分主要对节点行为和网络环境进行剧情设定，然后赋予了消息的加密属性，最后对大魔王的能力进行设定。

系统假设为异步分布式的，通过网络传输的消息可能丢失、延迟、重复或者乱序。作者假设节点的失效必须是独立发生的。

系统允许大魔王可以操纵多个失效节点、延迟通讯、甚至延迟正确节点来毁灭世界。但是作者限定大魔王不能无限期地延迟正确的节点，并且大魔王算力有限不能破解加密算法。

## 3.服务属性

!!! tip

    安全性靠异步保证（永远不出错），活性靠弱同步保证（最终能恢复）。

本文的算法弹性是达到最优的：当存在 $f$个失效节点时必须保证存在至少 $3f+1$ 个副本数量。

## 4.算法

PBFT 是一种状态机副本复制算法，即服务作为状态机进行建模，状态机在分布式系统的不同节点进行副本复制。
```

---

## 拆解要点

### 1. 开篇的"点睛之笔"
> "没错，这个 Loskov 就是提出著名的里氏替换原则（LSP）的人，2008年图灵奖得主。"

—— 提了一笔人物八卦，调动阅读兴趣。这是个很好的范式：**在严肃技术里穿插"花边"**。

### 2. `!!! tip` 的妙用
作者用 `!!! tip` 提炼**个人理解**。每章节开头或核心处必有 tip，把"作者悟到的本质"独立成块，让读者一眼看到重点。

### 3. 犀利评论风格
> "这些算法都是狗屎！PBFT 算法跟这些妖艳贱货完全不同..."

—— 不是骂人，是**有依据的吐槽**。作者对前人工作的否定+对 PBFT 的肯定，都基于论文原文给出的数据（性能提升一个数量级）。

### 4. LaTeX 公式
用 `$...$` 行内公式：`$3f+1$` / `$\frac{(n-1)}{3}$`。**不滥用**，关键数学处才用。

### 5. 章节内用 `!!! quote inline` 放论文原图
配合博客文章的 `images/` 目录引用截图，是**论文精读类**博客的固定模式。
