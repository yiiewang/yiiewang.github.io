# 复盘类范例：Go 服务幂等设计实战

> **原始文件**：`docs/blog/posts/2026/02/28.md`
> **类型**：线上事故复盘 + 工程实践
> **字数**：约 6000 字
> **特色**：故事开头 + 痛点列点 + 思路分层 + 数据模型落地

---

```markdown
---
date: 2026-02-28
authors:
  - cloaks
categories:
  - Golang 编程实践
tags:
  - Go
  - 后端工程
  - 幂等设计
  - 分布式系统
comments: true
description: 以支付回调为例，拆解 Go 后端中"防重复执行"的幂等落地方案，覆盖数据模型、事务边界与可观测性实践。
---

# 一次重复回调引发的库存事故：Go 服务幂等设计实战

## 引子

很多后端同学都遇到过这个场景：线上流量很稳，监控没报错，但运营突然来问，为什么同一个订单扣了两次库存。排查一圈后发现，请求本身不是"错"，而是"重复到了"。

在单机时代，"重复请求"通常只是日志里多一行；但在分布式系统里，重试、超时补偿、消息重放、网关抖动都可能让同一业务动作执行多次。幂等做不好，业务结果就会悄悄偏离预期。

<!-- more -->

## 问题

先把问题说具体。下面这类路径非常常见：

1. 用户支付成功
2. 第三方支付平台回调业务服务
3. 业务服务更新订单状态并扣减库存
4. 业务服务返回成功

理论上这条链路只该执行一次。但现实里，第三方回调可能因为网络抖动重发，或者我们返回慢导致对方判定超时并再次回调。于是同一个订单号，在短时间内被并发处理多次。

**症状通常有三种：**

- 库存被重复扣减
- 用户权益（积分、优惠券）被重复发放
- 下游系统收到多条语义相同但 ID 不同的事件

## 背景

很多团队早期会先做"表层防重"，比如在代码里判断 `if order.status == paid { return }`。这能挡住一部分串行重复请求，但挡不住并发竞争：两个请求同时读到 `unpaid`，都继续往下执行。

更本质地看，幂等不是 `if` 判断，而是**对业务状态机的约束**：

- 哪个字段代表"动作已生效"
- 哪些操作必须在同一事务里完成
- 重复到达时返回什么结果（成功复用、静默丢弃、明确拒绝）

## 核心

### 1）先定义幂等边界：以"业务动作"而不是"HTTP 请求"为单位

以"支付成功扣库存"为例，幂等键应该绑定业务语义：

- 推荐：`biz_type + biz_id`（如 `PAY_SUCCESS:order_123`）
- 不推荐：随机请求 ID（同一业务动作每次请求都不一样）

可以先落一张幂等记录表：

```sql title="idempotency_record.sql"
CREATE TABLE idempotency_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    biz_type VARCHAR(64) NOT NULL,
    biz_id VARCHAR(128) NOT NULL,
    status VARCHAR(16) NOT NULL DEFAULT 'processing',
    response_body TEXT,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL,
    UNIQUE KEY uk_biz (biz_type, biz_id)
);
```

这个唯一索引是第一道硬约束：同一个业务动作只能"首次占坑"一次。

### 2）状态机三态流转

`processing` → `success` / `failed`，**不允许从终态回退**。这样并发请求会卡在唯一索引上排队，避免脏写。

### 3）失败恢复

`processing` 状态卡住时（比如进程崩了），需要一个**超时回收机制**：

- 定时任务扫描 `processing` 超过 N 分钟的记录
- 标记为 `failed` 或重新放行
- 用分布式锁避免多个节点同时扫

### 4）可观测性

所有幂等命中要打点：

- 命中数 / 总请求数
- 重复请求的来源 IP / 调用方
- `processing` 超时回收次数

## 落地代码

```go title="idempotent.go"
// 伪代码：核心逻辑
func handleCallback(ctx context.Context, bizKey string, do func() (any, error)) (any, error) {
    // 1. 抢坑
    err := insertIdempotencyRecord(ctx, bizKey)
    if isDuplicateKeyError(err) {
        // 2. 已存在：等待原请求完成，复用其结果
        return waitForResult(ctx, bizKey)
    }
    
    // 3. 执行业务
    result, bizErr := do()
    
    // 4. 更新状态
    updateIdempotencyResult(ctx, bizKey, result, bizErr)
    return result, bizErr
}
```

## 注意事项

- ⚠️ 幂等键设计要绑定**业务语义**，不能绑定**请求 ID**
- ⚠️ 唯一索引是硬约束，不要用应用层 `if` 替代
- ✅ 失败状态要可恢复（超时回收、状态机可推进）
- ✅ 命中统计是可观测性的核心指标

## 小结

幂等不是单点技巧，是一套**约束设计**：

1. **数据层**：唯一索引作为硬约束
2. **业务层**：状态机单向流转
3. **恢复层**：超时回收 + 分布式锁
4. **观测层**：命中数 / 重复来源 / 状态分布

理解这一层后，再看其他分布式场景的幂等（消息消费、任务调度、API 重试），都是同一套思想的不同应用。
```

---

## 拆解要点

### 1. 标题有故事感
> "一次重复回调引发的库存事故：Go 服务幂等设计实战"

—— 不是"Go 幂等方案"，而是**点出具体事故**。读者一看就知道这是**真实踩过的坑**。

### 2. 引子的"共鸣感"
> "很多后端同学都遇到过这个场景：..."

直接点出**读者可能遇到的痛**，让读者觉得"这就是我"。

### 3. 痛点列点
> **症状通常有三种：**
> - 库存被重复扣减
> - 用户权益被重复发放
> - 下游系统收到多条语义相同但 ID 不同的事件

—— **列点比段落更清晰**。复盘文的痛点必须**显式列出**。

### 4. 解决思路分层
1) 先定义边界 → 2) 状态机 → 3) 失败恢复 → 4) 可观测性

—— 从"概念"到"细节"逐层递进。**每层都先讲为什么再做怎么做**。

### 5. 落地代码用伪代码
不追求完整可运行，强调**核心结构**。读者看完知道"原来是这样"，再去查具体 API。

### 6. 小结升华到方法论
> "理解这一层后，再看其他分布式场景的幂等...都是同一套思想的不同应用。"

—— 复盘文的高级写法：**从一个具体问题抽象出通用方法**。
